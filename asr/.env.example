# ASR Configuration
# Copy this file to .env and customize as needed

# Domain for Caddy reverse proxy
ASR_DOMAIN=asr.localhost

# Model cache directory (host path)
WHISPER_MODELS_PATH=/srv/yap/asr/models

# Whisper model to use (tiny, tiny.en, base, small, medium, large-v2, large-v3)
ASR_MODEL=tiny.en

# ASR engine (openai_whisper, faster_whisper)
ASR_ENGINE=faster_whisper

# Network name (for Caddy)
CADDY_NETWORK=caddy
