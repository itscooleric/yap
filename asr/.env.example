# ASR Configuration
# Copy this file to .env and customize as needed

# Domain for Caddy reverse proxy
ASR_DOMAIN=${QUICKYAP_ASR_DOMAIN:-asr.localhost}

# Model cache directory (host path)
WHISPER_MODELS_PATH=${QUICKYAP_ASR_MODELS_DIR:-/srv/whisper-asr/models}

# Whisper model to use (tiny, base, small, medium, large-v2, large-v3)
ASR_MODEL=${QUICKYAP_ASR_MODEL:-base}

# ASR engine (openai_whisper, faster_whisper)
ASR_ENGINE=${QUICKYAP_ASR_ENGINE:-openai_whisper}

# Network name (for Caddy)
CADDY_NETWORK=${QUICKYAP_CADDY_NETWORK:-caddy}
